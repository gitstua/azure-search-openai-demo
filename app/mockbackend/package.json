{
  "name": "mockbackend",
  "version": "0.1.0",
  "private": true,
  "description": "Minimal mock backend for frontend CSS development",
  "type": "module",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "start": "node server.mjs",
    "dev": "node --watch server.mjs"
  }
}
